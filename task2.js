class Player {
  constructor(name, level) {
    this.name = name;   // Task 1: propiedad name
    this.level = level; // Task 2: propiedad level
  }
}

// Exportar como objeto con la clave Player
module.exports = { Player };
